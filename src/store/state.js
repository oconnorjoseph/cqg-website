import {
  firebase,
  firestore,
  firebaseFunctions,
  firebaseStorage
} from "../util/firebase/index.js";

export default {
  firebase: firebase,
  firestore: firestore,
  firebaseFunctions: firebaseFunctions,
  firebaseStorage: firebaseStorage
};
